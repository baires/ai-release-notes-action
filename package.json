{
  "name": "ai-release-notes-action",
  "version": "1.0.4",
  "description": "AI-powered GitHub Action for generating release notes with optional integrations",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist --source-map --license licenses.txt",
    "package": "npm run build",
    "test": "jest",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/baires/ai-release-notes-action.git"
  },
  "keywords": [
    "github-action",
    "release-notes",
    "ai",
    "gemini",
    "slack",
    "automation"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/exec": "^1.1.1",
    "axios": "^1.6.0",
    "semver": "^7.5.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}